import React from 'react';
import SidebarTemplate from '../templates/SidebarTemplate';

const StackView = () => (
  <SidebarTemplate>
    <h1>Stack technologiczny, który miałem okazje poznać:</h1>
  </SidebarTemplate>
);

export default StackView;
